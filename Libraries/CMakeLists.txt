# Set up locations
set(BOOST_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/boost")

# Find dependencies
#find_package(Boost 1.69.0 REQUIRED
#	COMPONENTS asio mpi serialization
#	)
#include_directories(${Boost_INCLUDE_DIRS})
